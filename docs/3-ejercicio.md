# Ejercicio 3

- Crear un template (recordar de crear app/templates/app/...) que muestre una variable

- Crear una función que use render() para responder con el template creado, enviar una variable
que consista en una lista de notas (1 al 10)